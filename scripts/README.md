# Portfolio scripts

| Script | Purpose |
|--------|---------|
| `portfolio_stats.py` | Reads `docs/_generated/stats.json` after monorepo publish |

Canonical stats generation runs in CP Product Advisory:

```bash
python3 scripts/portfolio_stats.py
```
